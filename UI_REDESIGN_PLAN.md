# 《轮回》整套 UI 改造：明快卡通的 2.5D 场景叙事

- 文档日期：2026-09-06
- 状态：已按方案实施
- 代码基线：`9863e7a`
- 实施范围：拆分页面组件与展示适配层、2.5D 矢量场景、全部页面改造、展示层测试与设计文档同步。

## 1. 目标与视觉方向

采用已确认的 **2.5D 场景叙事、明快卡通、短叙事＋可展开详情**，覆盖全部页面。

使用 Cocos 2D 分层矢量绘制：背景、建筑、人物、前景道具通过遮挡、柔和阴影和轻动画形成空间感。人物具有年龄特征、动作和表情；配色采用奶油白、天空蓝、草绿与珊瑚橙，深色文字保证可读性。

主游玩页约 45% 空间用于场景，剩余区域组织状态、当前事件和行动。玩家应能迅速看懂“身在何处、发生什么、可以做什么”。

## 2. 页面与交互

| 页面 | 改造方式 |
| --- | --- |
| 首页 | 以轮回之门和人物作为主视觉；等级与经验缩为紧凑组件，传承显示为装备槽。自由、历史模式保留明确入口，进行中人生突出续玩。 |
| 天赋选择 | 图形徽章＋名称＋一句效果，完整说明可展开；已选天赋进入底部槽位，满足数量后可开始。 |
| 历史地域与人物 | 四域采用场景缩略图，十二位人物使用具有服饰、发型和代表道具的卡通肖像；选中后展示简介，再确认进入。 |
| 选路 | 展示最多三张场景预览卡，保留道路名称和一句描述，点击进入对应情景。 |
| 情景回合 | 大场景展示人物与活动；顶部显示回合和资源，底部显示完整可用行动及简短提示，点击行动后反馈实际结果。 |
| 关键抉择 | 保留当前场景，集中展示事件；同时列出 2—4 个选项供比较，选中后确认，预见与改写能力保留独立入口。 |
| 一程总结与人生结算 | 用场景收束、关键经历时间线、状态变化徽章和经验动画呈现结果；完整记录可展开。 |
| 奖励与传承装配 | 奖励采用三张图形收藏卡，选中后确认领取；装配页展示装备槽、收藏网格、阶数和完整说明。 |

八类情景分别绘制庭院、书房、集市、工坊、旅途、居所、议事厅和暮年庭院。历史模式复用场景骨架，按四域调整建筑、服饰和道具。

信息展示规则：

- 当前事件默认显示 2—3 行，超出部分提供明确的展开入口；剧情原文和因果记录完整保留。
- 光环、行囊、负累以图标和短名称显示，主界面最多展示三个，其余进入详情。
- 资源采用“图标＋名称＋真实数值＋变化反馈”；经验和回合使用进度条，未定义上限的资源不显示虚构百分比。
- 行动提示保留代价和风险要点；资源不足的既有行动显示禁用原因，未解锁行动继续遵循原有可见性规则。

## 3. 实现与兼容

- 将 `assets/scripts/GameApp.ts` 中集中创建页面的逻辑拆为页面组件、通用控件、场景绘制和纯数据展示适配层；`GameApp` 负责服务调用与页面调度。
- 新增按情景、历史地域和人物 ID 索引的视觉配置，以及场景、状态、选项的展示模型；不新增联网接口或修改存档格式。
- 场景依据当前状态绘制；操作完成后比较前后状态，驱动人物动作、道具变化与数值反馈。动画不参与随机计算，也不提前揭示未知结果。
- 同一情景内更新相关节点，切换页面时清理动画和监听；结算期间锁定提交，避免连续点击重复触发操作。
- 保留 720×1280 设计基准，增加安全区与屏幕比例适配；标题、正文、说明分别排版。长内容使用展开或局部滚动，取消依赖整块文字自动缩小的布局。
- 以实际显示尺寸保证主要触控区域至少 44×44 像素；滚动内容与底部操作区分离，防止遮挡。

## 4. 验证与交付

- 当前基线：类型检查及 21 项核心测试全部通过。改造后 `npm test` 为 27 项全部通过，覆盖随机一致性、退出续玩、历史章节、重复发奖保护，以及展示适配层的选项完整性、资源不足禁用、预见权限、长文本详情、状态差异和八类场景映射。
- 为展示适配层补充关键测试：选项不遗漏、资源不足禁用、预见权限、长文本详情、状态差异和八类场景映射。
- 使用独立测试存档验证自由与历史模式完整流程，以及天赋最多候选、四选项、传承满槽、升级、多条状态等拥挤情况。
- 在 320×568、375×812、430×932 和桌面窗口检查布局；验收无文字越界、重要内容截断、按钮遮挡或切页后动画残留。
- 重新生成 Web 与微信小游戏构建，交付关键页面截图，并同步设计文档。实施顺序为公共视觉与主游玩页、全部外围页面、完整流程与构建验收。

后续实施时使用项目已有的 `npm run build:web` 和 `npm run build:wechat` 生成构建。当前已有 Web 构建早于上述代码基线，不能作为改造后的验收产物；应以重新构建的页面为准。

工程运行和构建说明见 [README.md](README.md)，现有产品与玩法背景见 [Game_Design.md](Game_Design.md)。本计划记录已确认的 UI 改造方向。视觉资源、分层和控件仍适用；其中要求保留旧玩法入口、经验面板和历史模式流程的条款，已被后续 [玩法重构计划](GAMEPLAY_REFACTOR_PLAN.md) 取代。
