# Grok 4.6 实施提示词

将下面整段内容作为计划提交后的实施请求。完整规格以同目录的 `GAMEPLAY_REFACTOR_PLAN.md` 为准。

---

你现在负责在当前仓库内，完整实施用户已经确认的玩法重构。请直接修改代码、完成验证并提交实现；本轮不是继续讨论计划，也不是只交付设计说明。

## 任务与优先级

1. 首先读取 `GAMEPLAY_REFACTOR_PLAN.md` 全文，它是已批准的实现规格。阅读当前仓库的适用开发约定，核对实际代码和 `git status`、`git log`，然后开始工作。
2. 上一笔提交专门记录了本计划和本提示词。目标是把完整实现作为它后面的下一笔提交，不提交中间的计划、脚手架或半成品，不修改或压缩上一笔计划提交。
3. 本轮重构优先于 `Game_Design.md`、`README.md`、`UI_REDESIGN_PLAN.md` 中与新方案冲突的旧玩法、旧存档兼容和旧入口要求。现有视觉资源和分层仍然复用，完成后同步当前设计文档。
4. 用户已确认：人生点用于主动改变；过去影响反应但可以突破；跨世积累理解；行动积累并在关键处回望；每世 5—10 分钟；先完整重做自由模式；经历与理解完整替代旧成长；旧数据全部清理。不要重新询问这些决定，也不要退回兼容旧玩法的方案。

## 必须交付的实际行为

- 核心循环为“处境 → 遭遇 → 唤起往事 → 回应及代价 → 后果和碎片 → 回望 → 新遭遇 → 归档与下一世”。正常每世 6—8 次关键遭遇，第 3、6 次回应后各一次回望。
- 每次关键遭遇形成可追溯碎片，保存事实、实际回应、代价、相关往事和后果。关联到真实实例，分别表达事件来源和心理联想；不能用相似文案或共用选项 ID 假装因果。
- 人生点开局 2、上限 4，两次回望各加 1；突破惯常反应花 1，主动争取机会花 2。每次有 2—4 个选项，至少两个免费；点数不足也能继续；花点不保证成功；不跨世结转。
- 回望提供有实际经历支持的坚持、修正或存疑，三者给相同点数。修正保留旧事实和旧理解版本，并实际影响后续回应；新玩家没有跨世理解时也必须能完成第一次回望。
- 同世重点展开两条交叉人生线，后果优先于新随机遭遇。内容包括三个主题各六个节点和六个跨主题事件，总计 24 个关键遭遇模板，主题及验收见计划。每个选项都有关系、处境、承诺或机会上的实际后果；不能交付占位文本、全选项同结果或仅数值改名的内容。
- 普通模板同世不重复，跳过的年份推进世界变化。收束时兑现已知后果或说明未完成事项，不能为了凑 6—8 次而静默丢弃安排。
- 自动归档经历和理解，下世最多带入两条理解或疑问及其来源；支持不带。重复发现合并收藏展示，但保留实际来源链，不能把不同人生中的人物和事件混为同一实例。
- 不保留经验、等级、传承装配、三选一发奖、固定情景刷回合和阶段志向的可运行旧流程。历史入口隐藏，原始人物内容和相关美术保留为未启用内容；保留内容所必需的数据定义不等于保留旧引擎。
- 使用新 v3 存档。只清除本游戏旧的 `reincarnation-life.save.v1`、`reincarnation-life.save.v2`、`reincarnation-life.save.backup` 三个键，不迁移或备份旧进度，不全局清空存储，不删除有效 v3 存档。提供新版完整的自动保存、续玩和重复提交保护。
- 更新主页、遭遇页、回望页、因果详情、结尾及下一世准备流程。保留现有 Cocos 3.8.8、720×1280 竖屏、2.5D 场景、年龄人物和通用控件；旧资源面板和旧玩法入口不能残留在新流程中。所有关键规则必须在可玩的 UI 中可达。

## 工程实施要求

- 先确认 `core/`、`app/gameService.ts`、内容层、展示适配层、`GameApp.ts`、`ui/` 和存档层的实际调用关系。已知旧情景回合绕开了部分年度因果逻辑，不能只在旧界面附加碎片面板就视为重构完成。
- 领域层继续使用纯 TypeScript 与显式随机状态，不引入 Cocos、浏览器或微信 API；应用层统一校验命令、生成新状态并原子保存；UI 只通过命令操作状态。
- 待决状态保存遭遇实例、人物绑定、选项、点数成本、引用碎片和随机状态；重载不能换题或改价。查看档案和因果详情不能消耗点数、推进时间或改变随机状态。
- 对消费、回望和归档使用实例身份与状态检查，重复点击或重试不重复处理。内容校验覆盖引用、条件、免费选项、后果和回望来源。
- 内容采用本地配置与编写文本，不接入在线模型或新服务，不为本次改造更换引擎或构建工具链。
- 删除已废弃的实现和测试，保留仍适用的随机、存档事务、展示层和构建约束；不要为了让旧测试通过而维护与新设计冲突的功能。
- 将工程细节留在代码和开发文档中。游戏文案解释玩家的处境、记忆、代价和选择，不展示内部状态机、实例 ID 或测试术语。

## 验证与完成条件

以计划第 5 节为完整验收清单，至少完成：

1. `npm test`：包含新领域、内容、应用和展示层测试，以及 1000 局确定性模拟。旧基线 27 项通过只证明旧实现可运行，不能替代新玩法验收。
2. 用相同外部遭遇、不同经历验证回应差异和真实来源；验证不同回应产生不同后果，理解修订会影响后续行为。
3. 覆盖零点继续、超支拒绝、两次回望、重复提交、归档去重、因果图引用、跨世来源和全部待决状态重载。
4. 验证旧存档三键清理、其他数据保留、新 v3 二次启动可续玩。
5. `npm run build:web`：使用本次代码生成的新产物，不用仓库中已有的旧构建冒充结果。
6. 在浏览器竖屏完整体验至少三种回应风格，并连续体验三世，核对 5—10 分钟节奏、代价、回响、回望、档案与下一世携带理解。检查长文本、最多四个选项、因果详情返回和连续点击；记录实际观察结果。
7. 同步 `Game_Design.md`、`README.md` 和计划实施状态，准确记录新规则、测试、构建及实际限制。

先修复实现、内容和验证发现的问题，再提交。若引擎或浏览器环境确实阻止某项检查，先排查并尽可能完成其他检查，在交付中具体说明命令、错误和未验证部分；不能声称未执行的检查已通过，也不能用一份部分完成的实现替代完整玩法。

## Git 交付

- 开始时记录计划提交的完整 SHA。保持它不变；本轮仅创建一笔完整实现提交，建议标题为 `feat: rebuild life gameplay around experiences and meaning`。
- 提交前检查工作区和完整 diff，只暂存本任务源码、内容、测试和文档。不要提交 `build/`、`temp/`、`library/`、`.test-dist/`、依赖目录或其他生成产物，也不要混入用户的无关改动。
- 如果期间出现他人的新提交，不重置或改写他人历史；检查最新基线并如实报告与“紧接计划提交”这一预期的偏差。
- 最终回复说明实现提交号、核心玩法变化、测试及构建结果、浏览器实测情况和确切的未完成验证项。代码和玩法必须已经实现，不要只返回下一步计划。
- 用户要求本地提交，不要求推送、部署、上传微信或对外发布；不要自行执行这些外部操作。
